═══════════════════════════════════════════════════════════════════
  ✅ BABEL PROBLEM - REŠENJE
═══════════════════════════════════════════════════════════════════

PROBLEM:
  Cannot find module 'babel-plugin-module-resolver'

UZROK:
  babel.config.js koristi plugin koji nije instaliran u package.json

REŠENJE:
  Dodao sam 'babel-plugin-module-resolver' u devDependencies

───────────────────────────────────────────────────────────────────
ŠTA TREBA DA URADIŠ:
───────────────────────────────────────────────────────────────────

KORAK 1: Obrisi node_modules (ako već postoji)
───────────────────────────────────────────────

Windows (Command Prompt):
  rmdir /s /q node_modules
  del package-lock.json

Windows (PowerShell):
  Remove-Item -Recurse -Force node_modules
  Remove-Item package-lock.json

Mac/Linux:
  rm -rf node_modules package-lock.json


KORAK 2: Preuzmi ažurirani package.json
────────────────────────────────────────

Preuzmi NOVI package.json sa servera (ovaj koji ima babel-plugin-module-resolver)

ILI ručno edituj package.json i dodaj u devDependencies:
  "babel-plugin-module-resolver": "^5.0.0"


KORAK 3: Instaliraj dependency-je
──────────────────────────────────

  npm install


KORAK 4: Obrisi keš (VAŽNO!)
─────────────────────────────

  npx expo start --clear


KORAK 5: Pokreni aplikaciju
────────────────────────────

  npm start

───────────────────────────────────────────────────────────────────
BRZE KOMANDE (copy-paste):
───────────────────────────────────────────────────────────────────

Windows:
  rmdir /s /q node_modules
  del package-lock.json
  npm install
  npx expo start --clear

Mac/Linux:
  rm -rf node_modules package-lock.json
  npm install
  npx expo start --clear

───────────────────────────────────────────────────────────────────
ŠTA SAM PROMENIO:
───────────────────────────────────────────────────────────────────

1. package.json - Dodao u devDependencies:
   "babel-plugin-module-resolver": "^5.0.0"

2. babel.config.js - Ispravio naziv plugina:
   BILO: 'module-resolver'
   SADA: 'babel-plugin-module-resolver'

───────────────────────────────────────────────────────────────────
VERIFIKACIJA:
───────────────────────────────────────────────────────────────────

Kada pokreneš npm start, VIŠE NE BI TREBALO da se pojavi greška
o babel-plugin-module-resolver!

Trebalo bi da vidiš QR kod normalno.

───────────────────────────────────────────────────────────────────
AKO I DALJE NE RADI:
───────────────────────────────────────────────────────────────────

1. Proveri da li je babel-plugin-module-resolver instaliran:

   npm list babel-plugin-module-resolver

   Trebalo bi da vidiš verziju 5.0.0

2. Obrisi .expo folder:

   Windows:
     rmdir /s /q .expo

   Mac/Linux:
     rm -rf .expo

3. Obrisi SVE i kreni ispočetka:

   rm -rf node_modules package-lock.json .expo
   npm install
   npx expo start --clear

───────────────────────────────────────────────────────────────────
TEHNIČKI DETALJI:
───────────────────────────────────────────────────────────────────

babel-plugin-module-resolver omogućava:
  - Aliase u importima (npr. @/components umesto ../../../components)
  - Lakše referenciranje fajlova
  - Čistiji kod

U našem slučaju koristi se za:
  alias: {
    '@': './src'
  }

Što znači da možeš pisati:
  import Component from '@/components/Component'

umesto:
  import Component from '../../../src/components/Component'

───────────────────────────────────────────────────────────────────

🎉 Preuzmi novi kod, obrisi node_modules, instaliraj i pokreni!

═══════════════════════════════════════════════════════════════════
